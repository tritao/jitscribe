[package]
name = "jitscribe-audio-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Cross-platform audio capture core for Jitscribe"

[dependencies]
cpal = { version = "0.18.2", features = ["pipewire", "pulseaudio"] }
